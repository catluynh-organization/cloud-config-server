server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/catluynh-organization/cloud-config-server-sinhvien-khoa-
          default-label: main
          bootstrap: false
          clone-on-start: true

